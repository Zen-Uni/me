/**
 * @description ButtonSlot 固定样式，方便扩展组件的样式定位
 * @author Uni
 */

import styled from "@emotion/styled";

export const ButtonSlotWrapper = styled.div`
    position: absolute;
    z-index: 100;
    bottom: 30px;
    right: 15px;
`